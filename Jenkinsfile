pipeline {
    agent any

    stages {
        stage('Build & Run') {
            steps {
                sh '''#!/bin/bash
                    docker --version
                    '''      
            }
        }

    }
}
